/# Ignore only root lockfile, allow app lockfile
/package-lock.json
!portfolio-react/package-lock.json

# Node modules
node_modules/
dist
.env
.env.local
.vscode

# Vite cache for React app (avoid merge conflicts)
portfolio-react/.vite/